using UnityEngine;

public class Probando1 : MonoBehaviour
{
    public Rigidbody2D rb;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        rb.gravityScale = 0;
    }
    // Update is called once per frame
    void Update()
    {
          
    }
}
